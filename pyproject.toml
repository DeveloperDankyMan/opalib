[project]
name = "opalib"
version = "0.1.0"
description = "A library that can do many things."
authors = [
    { name="Donovan", email="donovandelisle7@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
